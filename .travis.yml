language: java
os: linux
dist: xenial
jdk: oraclejdk11
env: TERM=dumb
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
before_install:
  - nvm install 12
  - npm ci
install:
  - ./gradlew assemble
script:
  - echo "Testing goes here"
jobs:
  include:
    - stage: release
      script: skip
      deploy:
        provider: script
        script: npm run semantic-release